using UnityEngine;

public class CharacterCarryState : CharacterState
{
    
    public CharacterCarryState(Character @char, StateMachine stateMachine) : base(@char, stateMachine)
    {
        
    }

    private void SetToCarryState(Vector3 value)
    {
        if (value)
    }
    
}